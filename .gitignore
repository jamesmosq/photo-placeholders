.DS_Store
.gradle
.idea
.intellijPlatform
.kotlin
build

# Plugin signing material. The real keys live OUTSIDE the repo in ~/.photo-placeholders-signing/;
# these patterns are a safety net in case a copy ever lands in the project folder.
.photo-placeholders-signing/
*.pem
*.key
password.txt
chain.crt
